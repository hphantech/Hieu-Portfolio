# Hieu Phan — Portfolio

A personal portfolio site built with **Next.js**, **TypeScript**, and **Tailwind CSS**.

## Getting started

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) to view the site locally.

## Project structure

```
src/
├── app/              # Next.js App Router pages and layout
├── components/       # Reusable UI components
└── lib/site.ts       # Site content and configuration
```

Update copy, links, and project data in `src/lib/site.ts`.

## Scripts

- `npm run dev` — start the development server
- `npm run build` — create a production build
- `npm run start` — run the production server
- `npm run lint` — run ESLint

## Roadmap

- [ ] Add real project case studies
- [ ] Connect GitHub and LinkedIn profiles
- [ ] Add an about page with study background
- [ ] Improve accessibility audit and keyboard navigation
- [ ] Deploy to production (Vercel recommended)
